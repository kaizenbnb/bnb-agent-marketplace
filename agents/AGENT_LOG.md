> El trabajo de construcción de los 4 agentes (indexer ERC-8004, wallets, scripts contra Venus/PancakeSwap) vive en la carpeta [`agents/`](./) de este repo: [`agents/altana-agent/`](./altana-agent/) para los scripts, [`../indexer/`](../indexer/) para el indexer. Este es el log completo de ese build.

# AGENT_LOG: BNB "Build the Era"

Registro vivo. Una entrada por tropiezo. Actualizar **antes** de cada paso siguiente.

| Error | Causa | Fix | Lección |
|---|---|---|---|
| Asumí que el ABI del registry era el del contrato en la dirección publicada | La lógica no vive ahí: los Identity/Reputation Registry son **proxies EIP-1967** (el bytecode empieza por el slot `0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc`); la implementación real está en otra dirección | Leer el slot 1967 / usar el ABI de la **implementación**, no el del proxy; para enumerar y leer feedback tirar de ABI estándar ERC-721 + interfaz ERC-8004, no del bytecode del proxy | Todo registry ERC-8004 en BSC es un proxy. `totalSupply()` funciona en BRC8004 (26 agentes) pero **revierte** en el oficial `0x8004A169…` → ese hay que muestrearlo por eventos `Registered`, no por llamada directa |
| Asumí que el registro oficial era pequeño (mismo orden que BRC8004) | Los agentIds muestreados empezaban en 263.322; `ownerOf(263.400)` ya revierte → el registro oficial tiene **~263.000 agentes históricos**, no decenas | Sondear `ownerOf` en intervalos exponenciales para estimar el tamaño total antes de decidir estrategia de indexado | El registro oficial está en PLAN A (>500). El challenge real es calidad, no cantidad: ~40% de los 40 recientes son bots de Quack AI. Filtrar por card resoluble + nombre legible |
| `getLogs` colgaba en todos los RPCs públicos de BSC | Los dataseed.bnbchain.org / defibit.io rechazan getLogs con "limit exceeded" incluso para 1 bloque; publicnode soporta getLogs pero solo con chunks ≤5000 bloques | Separar clientes RPC: `rpc_call` (dataseed válidos para eth_call) y `rpc_logs` (solo publicnode/1rpc para getLogs); chunk 2000 bloques; ventana de actividad limitada a 60k bloques (~7.5h) en el probe rápido | Para un indexador completo (30d de historia) se necesita RPC de archivo privado (QuickNode, Alchemy, etc.). Los públicos son solo para el gate/prototipo |
| Clasificar 52 agentes por card viva dio 0/4 categorías del rubric (rebalancing, grid, yield, health factor) | La estrategia de muestreo temporal (eventos Registered más recientes) captura el "frente de registro" del momento: ahora mismo son Quack AI bots (pagos gasless, 12 instancias), EvoEvo clones (IA genérica/deportes, 22 instancias) y FOUR-LIFE meme tokens (6 instancias). Los agentes DeFi-nativos que implementan las 4 categorías existen en el universo de 263K, pero NO en los últimos N registros | 8004scan API tiene búsqueda semántica (`GET /agents/search?q=grid+trading`) y acceso por categoría/tag. Sustituye al indexer manual como fuente principal: indexar RPC era el paso "no queda más remedio"; con 8004scan se puede filtrar directamente por descripción/skill. El Free tier (100 req/min) basta para S2 | **Muestrear por bloque reciente = capturar el frente de registro (factories), no el universo de categorías. Para cobertura por categoría: búsqueda semántica, no barrido temporal.** |
| Asumí que el gate S2 requería encontrar agentes ya etiquetados en las 4 categorías | La página del hackathon define: el entregable es un **marketplace frontend** ("Build the BNB Agent Studio Marketplace"), no agentes. Las 4 categorías (Rebalancing, Grid Trading, Yield Optimisation, Health Factor Monitoring) son el rubric del marketplace, lo que debe poder mostrar, no etiquetas que los agentes tienen previamente. BNB NO publica reference agents. El indexer buscó 1.450+ agentes BSC sin encontrar ninguno con esas categorías en su descripción porque **ningún agente en el registro las declara en texto libre** | Las categorías son la capa de clasificación que nosotros construimos, no datos que el registry provee. El marketplace necesita: (1) heurística propia de clasificación por descripción+card+skills, o (2) curación manual de un catálogo semilla, o (3) integrar directamente con los proveedores (Ave.ai, XORR, Termix) que SÍ construyen esos agentes pero con descripciones genéricas | **S2 gate real: ¿puede el marketplace mostrar ≥1 agente vivo por categoría con datos suficientes para que un usuario tome una decisión? La clasificación es trabajo nuestro, no del registry.** |
| Despliegue de agente Venus Lending (testnet): el faucet oficial de BNB pide CAPTCHA en el submit | `testnet.bnbchain.org/faucet-smart` exige 0.002 BNB en mainnet en la wallet destino (anti-sybil) Y dispara un puzzle visual (grid de letras) al pulsar "Send". Wallet ya creada y lista: `0x3f94Fd4772345DF0EADF2284c845855cA7e18b62` (BNB Testnet, chain 97), solo falta fondear | Regla dura: nunca resolver CAPTCHAs, ni con petición explícita del usuario. Paro el flujo automatizado en el paso 3/6 (fondeo) y lo devuelvo a Oliver: o resuelve el CAPTCHA él, o fondea desde una wallet propia con tBNB, o usa un faucet alternativo (QuickNode/Chainstack, listados en la misma página, posiblemente sin CAPTCHA) | **El faucet es fricción humana por diseño (anti-bot). Cualquier pipeline de "agente autónomo que se autofinancia en testnet" choca aquí: no es un bug del script, es la barrera intencional. Para demos repetibles: pre-fondear una wallet fija una vez y reusarla, no generar una nueva por ejecución** |
| `grantSession` falla con `invalid auth item nonce, expected 1, got 0` (persistente, 2 intentos) tras fondear la wallet | Minté 100 USDT de test llamando a `allocateTo()` con una tx **raw de viem** firmada con la misma clave admin, FUERA del flujo de Altana (`walletClient.writeContract` directo, no `client.execute`). Eso subió el nonce onchain de la EOA de 0 a 1. El SDK `@altananetwork/sdk@0.7.0` construye la autorización EIP-7702 (delegación de código a la smart account) asumiendo siempre nonce=0: no la calcula dinámicamente contra el estado real de la cadena. El relay valida el nonce real (1) contra la autorización firmada (0) y rechaza, en los dos intentos, con timestamps de quote distintos (no era un problema de cache) | La wallet `0x3f94Fd...` queda inutilizable para el flujo Altana: no hay parámetro para forzar el nonce de autorización. Hay que generar una wallet nueva y, esta vez, **enrutar absolutamente todo, incluido el mint de USDT de prueba, a través de `client.execute()` (admin path), nunca con una tx raw de viem/ethers** aunque solo sea para "sembrar" balance antes de la sesión | **Con Altana (arquitectura EIP-7702 delegada), la primera transacción de una EOA recién creada DEBE pasar por el SDK. Cualquier tx raw previa (aunque sea trivial, aunque use la misma clave) rompe la cuenta para siempre con ese proveedor. No mezclar flujos raw + smart-account sobre la misma dirección** |
| Al necesitar wallet 2, pedí a Oliver un segundo fondeo (0.002 BNB mainnet) en vez de recuperar el saldo real ya enviado a wallet 1 | Wallet 1 seguía teniendo 0.0025 BNB mainnet + 0.3 tBNB testnet reales: abandonarla y pedir fondear otra desde cero habría dejado dinero real huérfano en una wallet cuya clave privada solo yo tengo. Oliver lo señaló directamente: "no puedes fallar en eso ni abandonar wallets con dinero real" | Con la clave privada de wallet 1 (que sigo teniendo, solo está rota para el flujo *Altana*, no para transferencias EOA normales) hice un rescate: dos txs raw simples (mainnet BNB y testnet tBNB) de wallet1 -> wallet2. Cero fricción nueva para Oliver, cero fondos perdidos | **Cuando genero una wallet nueva porque la anterior queda inservible para un propósito específico, la clave sigue siendo mía y cualquier saldo real en ella es recuperable: rescatar antes de pedir un fondeo nuevo, siempre. Nunca tratar una wallet con fondos reales como desechable** |
| `execute` del play "supply" falla con `NoSpendPermissions` en el `approve()` | El `grantSession` inicial solo declaraba un `SpendPermission` nativo (tBNB, sin `token`). Aunque `approve`/`mint` mandan `value: 0n` (no gastan nativo), el validador onchain de Altana reconoce selectores tipo `approve`/`transfer` como "gasto de token" y exige un `SpendPermission` explícito con `token: <direccion>` para ESE ERC-20, independiente del cap nativo | Se reggranteó la sesión añadiendo `{limit: 100 USDT, period: "day", token: USDT_ADDRESS}` junto al cap nativo. Con eso el `approve()+mint()` pasó y quedó confirmado onchain (tx `0x1b2153d0...`) | **En Altana, `permissions.spend` no es "cuánto BNB puede gastar la sesión" sino "qué tokens puede mover, y cuánto, cada uno explícito". Cualquier ERC-20 que la sesión vaya a aprobar/transferir necesita su propia entrada con `token`: el cap nativo no cubre ERC-20s** |

## Resultado: agente Venus Lending desplegado en BNB Testnet

- Wallet agéntica: `0x5bc1C0779fC435f5C8Dd2692E667e51716e1e9fb` (chain 97)
- Posición confirmada: **100 USDT suministrados en Venus Core Pool** (vUSDT)
- Tx de supply: `0x1b2153d048a8c0b40ec6156452fa54f18fa4eb8b5e3f4e5a5412108eae5481b0`
- Verificado por lectura directa onchain (no solo por el relay): `balanceOfUnderlying` recompuesto = 100.000000 USDT
- Explorer: https://testnet.bscscan.com/address/0x5bc1C0779fC435f5C8Dd2692E667e51716e1e9fb
- Scripts en `altana-agent/` (wallet1 rota conservada en `.env.wallet1-broken` como evidencia del bug de nonce)

| Error | Causa | Fix | Lección |
|---|---|---|---|
| El "comparador de yield" (Venus/Aave/Lista) no se puede probar de verdad en testnet | Verifiqué (mismo gate que con Venus) si Aave V3 y Lista tienen deployment en BSC testnet chain 97: **Aave V3 no** (el address-book oficial `bgd-labs/aave-address-book` tiene `AaveV3BNB.sol` mainnet pero ningún `AaveV3BNBTestnet.sol`); **Lista tampoco** (su propia skill de Altana solo documenta mainnet, sus repos no exponen direcciones de testnet, y las direcciones mainnet no tienen bytecode en chain 97) | Con Oliver: desplegar "yield agent v1" con arquitectura para 3 protocolos (cada fuente sabe leer su tasa) pero en testnet solo la rama Venus ejecuta: Aave/Lista quedan marcadas `available: false` con motivo explícito, listas para activarse cambiando `CHAIN_ID` a 56 en mainnet | **No todos los protocolos DeFi tienen testnet: Venus es la excepción, no la norma. Cualquier "agente multi-protocolo" debe verificar disponibilidad por chain ANTES de prometer cobertura, y degradar con gracia (marcar no-disponible, no fallar en silencio) cuando falte una pata** |

| Health factor agent: `borrow()` de 90% del máximo mostró "0.000000 USDT" y un HF de 2.7 billones (imposible) | Dos bugs distintos, uno enmascarando al otro. (1) Asumí que el USDT de testnet tenía 18 decimales, copiado del SKILL.md de Venus, que documenta **mainnet**. Nunca llamé `USDT.decimals()` en el contrato real de testnet. Verificado ahora: **son 6 decimales, no 18**. (2) Como consecuencia, TODOS los mints/supplies de USDT en los agentes anteriores (scripts 1-5, "100 USDT" + "50 USDT" = "150 USDT" reportados a Oliver) en realidad movieron `parseUnits(n, 18)`: con un token de 6 decimales eso es `n * 1e12` unidades reales. La "posición de 150 USDT en Venus" reportada como cierre de la sesión anterior es en verdad **150 BILLONES (150,000,000,000,000) de USDT de test**, verificado leyendo `balanceOfUnderlying` en crudo y dividiendo por 1e6, no por 1e18. Dinero de juguete sin valor real, pero el reporte a Oliver fue numéricamente falso | Pendiente de decidir con Oliver: limpiar la posición legado (redeem) antes de terminar el health-factor agent, o aceptar el estado y documentar la distorsión | **Nunca asumir decimales de un token, ni siquiera citando el SKILL.md oficial, que documenta mainnet, no testnet. `decimals()` es una llamada de lectura gratuita: verificar SIEMPRE contra el contrato real que se va a tocar, no contra la documentación de otra red.** El borrow() en sí (43.2 USDT reales) SÍ salió bien dimensionado porque la fórmula basada en oráculo es decimal-agnostic por diseño Compound: el bug estaba solo en el display y en los mints previos, no en el cálculo de riesgo del borrow |

| `redeem()` y `repayBorrow()` fallan con `NoSpendPermissions` sea cual sea el `SpendPermission` declarado (probado con cap exacto y con `2**200n`) | Diagnóstico por eliminación: `mint`, `borrow` y `enterMarkets` funcionaron correctamente vía sesión scoped en los pasos anteriores. Solo `redeem`/`repayBorrow` fallan, siempre con el mismo error byte-a-byte, sin importar el tamaño del cap declarado, descartando que sea un problema de dimensionamiento. Probé la MISMA llamada (`approve`+`repayBorrow`) por el **path admin** (`client.execute({wallet, signer, calls})`, sin sesión) y funcionó a la primera | Usar el path admin para estas dos funciones específicas en vez de una sesión scoped, hasta que Altana las soporte (o hasta encontrar la sintaxis correcta de permiso, si existe) | **El validador de sesiones de Altana (`@altananetwork/sdk@0.7.0`) reconoce `approve`/`transfer`/`transferFrom`/`mint` como movimientos de token válidos bajo `SpendPermission`, pero `redeem` y `repayBorrow` de un cToken Compound-fork no están soportados vía sesión: ninguna combinación de permisos los desbloquea. Es una limitación de la plataforma, no un error de configuración. Para agentes que necesiten deshacer posiciones (redeem/repay), el path admin es la única vía hasta que el SDK lo cubra** |

| Grid v1: pool WBNB/USDT-testnet-Venus con liquidez real, pero un `swap` de 20 USDT movió el precio 0.00% en 10 ticks | El reserve0 (USDT) del pool era 670.209.689.828,53 unidades: otro equipo/bot cometió el MISMO bug de decimales (18 en vez de 6) al sembrar liquidez en ese par. Contra un pool inflado a esa escala, cualquier trade de tamaño humano es polvo sin impacto de precio medible | Busqué pares alternativos WBNB/stablecoin en el mismo Factory: BUSD-testnet oficial (12.5 WBNB de reserva) y USDT-testnet oficial (17 WBNB) están razonablemente escalados. Cambié al par WBNB/BUSD | **Antes de fijar el par para un grid bot, comprobar el ORDEN DE MAGNITUD de las reservas, no solo que el par exista. Un pool "con liquidez" puede estar inflado por el mismo bug de decimales que uno mismo acaba de cometer en otro sitio: verificar reservas en unidades humanas, no en raw** |
| Grid v2 (pool bien escalado): con capital real (~0.2 tBNB) contra un pool de 12.5 WBNB, el paso de rejilla original (4%) era inalcanzable | Impacto de precio de una operación ≈ tamaño_orden / reserva_pool. Con 0.2 tBNB contra 12.5 WBNB, ni la mejor operación individual movía el precio ni siquiera un 2% | Recalibré: paso de rejilla a 0.4%, tamaño de orden a 0.05 BNB (~15-20% del capital total por operación, ~0.4% de impacto esperado, resultó ~0.76% real por la curvatura del producto constante) | **El tamaño de rejilla no es una elección de diseño libre: está acotado por capital_propio / profundidad_del_pool. Un grid bot con poco capital contra un pool líquido necesita una rejilla MUY estrecha (decimales de %), no la separación amplia (4-8%) que parece "razonable" a primera vista** |
| El loop reactivo (dispara solo si la celda cambia desde la última lectura) nunca arrancaba: 10 ticks seguidos a 0.00% | Diseño puramente reactivo: sin actividad externa en el pool durante la ventana de prueba (testnet de bajo tráfico) y sin holding inicial del token de cotización (BUSD), no había NADA que disparase el primer cruce: problema del huevo y la gallina | Añadí un "kick-off": la primera acción del agente es una venta forzada (BNB->BUSD) para generar inventario y arrancar el movimiento de precio; a partir de ahí el loop reacciona con normalidad a los cruces que sus propias operaciones producen | **Un grid bot recién desplegado contra un mercado sin actividad garantizada necesita una operación semilla explícita, no solo lógica reactiva pura: "vigilar y esperar a que algo cruce" no basta si nada más está operando en el pool** |

| El USDT-oficial de testnet tiene `mint(uint256)` pero revierte con "Ownable: caller is not the owner" | A diferencia del mock de Venus (`allocateTo`, público), este token restringe el mint al owner del contrato: no todo mock de testnet es auto-servible | Conseguí el token de la forma normal: swap de tBNB -> USDT-oficial en el Router V2 ya verificado, en vez de mint directo | **No asumir que un token de testnet tiene faucet/mint público solo porque otro (Venus) lo tenía. Probar por simulación (`simulateContract`) antes de construir el flujo alrededor de un mint que puede no existir** |

## Resultado: agente Rebalancing desplegado en BNB Testnet (PancakeSwap V3)

- Gate de infraestructura en dos partes, ambas verificadas antes de escribir código:
  1. PancakeSwap V3 SÍ está desplegado en BSC testnet: Factory `0x0BFbCF9fa4f9C56B0F40a671Ad40E0805A091865` (misma en todas las chains), NonfungiblePositionManager `0x427bF5b37357632377eCbEC9de3626C71A5396c1` (**distinta de mainnet**, hay que usar la de testnet explícitamente)
  2. De los pools candidatos: WBNB/BUSD V3 tiene liquidez=0 en los 4 fee tiers (vacíos, ticks en el extremo -887272/887271, nunca inicializados con liquidez real). WBNB/USDT-oficial al 0.25% SÍ tiene liquidez real y sana (0.59 WBNB / 9 USDT, tick -24427, no inflado como el par V2 de la sesión de grid)
- No hay skill de Altana para liquidez V3 (solo V2) ni para rebalancing: todo compuesto a mano contra el NonfungiblePositionManager
- Secuencia ejecutada, sin bucle, un solo ajuste real:
  1. Swap 0.03 tBNB → USDT-oficial (V2 Router, ya que el mint directo estaba restringido a owner)
  2. Wrap 0.02 tBNB → WBNB
  3. Mint posición A: rango `[-24950, -23900]` (±500 ticks, 10 tickSpacings), tokenId `36781`, liquidez `2624525267694961440`, tx `0xd4b3d1a5f51960da3086d134017f9af8487229d11b90c2419dc97738012d857b`
  4. **Ajuste real**: `decreaseLiquidity` + `collect` sobre A (cierre completo, recupera 0.407346 USDT + 0.020000 WBNB exactos), tx `0x485da2875edf17055307b52f4e18e47cdda5b419ce64453a02787f7fe7e022df`
  5. Reposicionar: mint posición B con los mismos fondos recuperados, rango desplazado `[-24700, -23650]` (+250 ticks vs A), tokenId `36782`, liquidez `3155047326754856497`, tx `0x5ae725b19bccdf05f256051493170eea5c00f20f0386f1f4f3187dd1fecebd24`
- Las 5 transacciones confirmadas a la primera, sin bugs de sesión ni de decimales (las lecciones de las 4 sesiones anteriores se aplicaron correctamente desde el diseño)
- Coste real: ~40 min. Desglose: 10 min gate de infraestructura (2 partes) + 10 min encontrar cómo conseguir USDT-oficial (mint restringido, pivote a swap V2) + 15 min diseño del script (tick alignment, orden token0/token1, extracción de tokenId desde logs Transfer) + 5 min ejecución

## Resultado: agente Grid Trading desplegado en BNB Testnet (PancakeSwap V2)

- Gate de infraestructura: PancakeSwap V2 SÍ tiene deployment oficial en BSC testnet: Factory `0x6725F303b657a9451d8BA641348b6761A6CC7a17`, Router `0xD99D1c33F9fC3444f8101754aBC46c52416550D1` (confirmado en developer.pancakeswap.finance, no en la skill de Altana que solo documenta mainnet)
- Par usado: WBNB/BUSD-testnet oficial (reserva 12.5 WBNB, razonablemente escalado)
- Rejilla: 5 niveles, paso 0.4%, orden 0.05 BNB
- Kick-off (venta forzada, sin holding inicial de BUSD): tx `0xa5c689a0a3684935cf6d1446eaad9a3903c8471f152be9cd1b42debd58706e91`, precio cruzó de celda 0 a celda -2 (-0.763%), tocando el límite de la rejilla
- 1 cruce real detectado y ejecutado correctamente por la lógica de umbrales (no hardcodeado); sin cruces adicionales en la ventana de prueba por falta de actividad externa en el pool, comportamiento honesto del mecanismo, no un fallo
- Coste real: ~55 min. Desglose: 10 min gate de infraestructura (PancakeSwap sí tiene testnet), 15 min encontrar y descartar el par WBNB/USDT-Venus (inflado), 15 min recalibrar tamaño/paso de rejilla para el par bueno, 15 min diseño e implementación del kick-off + ejecución final

## Resultado: agente Health Factor desplegado en BNB Testnet (Venus)

- Misma wallet. borrow/repay compuestos a mano contra el Comptroller (Unitroller) y vUSDT, no están en la skill oficial de Venus Lending (solo-supply)
- Posición creada: 0.05 tBNB como colateral (`vBNB.mint` payable) + `enterMarkets` + `borrow` de 43.2 USDT reales (90% de la capacidad, calculado desde `getAccountLiquidity` real, no adivinado)
- **Hallazgo retroactivo importante**: el USDT de testnet tiene **6 decimales**, no 18 como documenta el SKILL.md de Venus (que es de mainnet). Los agentes anteriores (Venus + Yield) mintearon y reportaron "150 USDT" que en realidad son 150 billones de unidades de test, sin valor real, pero el número reportado a Oliver era incorrecto. Ver entrada de arriba
- `getAccountLiquidity()` quedó contaminado por esa posición legado de escala 1e12; el intento de limpiarla vía `redeem()` chocó con la limitación de sesión de arriba. Se resolvió calculando el health factor de forma **aislada** (solo colateral BNB real vs deuda real, leído a mano desde `vBNB.balanceOf`+`exchangeRateStored`+precio del oráculo vs `borrowBalanceStored`+precio), sin depender de la lectura contaminada
- HF aislado inicial: **0.9722** (por debajo de 1.0, posición genuinamente en riesgo, no simulada), umbral configurado: 1.15
- Acción protectora ejecutada (repago parcial 30% de la deuda, vía path admin tras el bloqueo de sesión): tx `0x557c3171ea77fba6c53ccaafbd73719589c5d147c9977b158201c222363392c1`
- HF final verificado: **1.3889**, mejora confirmada, posición fuera de zona de riesgo
- Coste real: ~70 min. Desglose: 15 min investigación de ABI/Comptroller/oráculo (necesaria, borrow/repay no están en ninguna skill), 20 min debugging el bug de decimales retroactivo, 25 min debugging la limitación de sesión en redeem/repayBorrow (3 intentos fallidos + 1 diagnóstico que aisló la causa), 10 min ejecución real una vez resueltos ambos bloqueos

## Resultado: agente Yield desplegado en BNB Testnet

- Misma wallet, sesión nueva scoped a USDT+vUSDT (Aave/Lista no aplicables en esta chain)
- Comparador leyó 3 fuentes, 1/3 disponible (venus), decidió y ejecutó sin intervención
- 50 USDT sembrados + suministrados: tx `0x3cb2f287b53d26077d4169638910ecb7d4b42319899d2e8f9d823ccd3f527672`
- Posición acumulada total en Venus: **150 USDT** (100 del agente anterior + 50 de este)
- **Cero bugs nuevos**: las dos lecciones de nonce y spend-permission-por-token se aplicaron correctamente a la primera
- Coste real: ~20 min, casi todo en verificar Aave/Lista (la ejecución en sí tardó ~2 min una vez decidido el diseño)
